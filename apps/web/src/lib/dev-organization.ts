/**
 * Fixed identifiers for the local development tenant.
 * This is not production organization resolution and must be replaced when
 * auth and real org context exist.
 */
export const DEV_ORGANIZATION_ID = "dev-org-id" as const;
export const DEV_ORGANIZATION_NAME = "Development Organization" as const;
